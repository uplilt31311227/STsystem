# 工作交接紀錄

> 日期：2026-06-27
> 專案：STsystem（國中調代課自動化系統）
> 分支：`feature/permission-system`

## 當前計畫
v2.0.0 多角色升級（PLAN_v2.0.0.md）— 卡在 **Phase 1 線上實機驗收**。本次目標：把最新 V2 部署到線上 preview 子站讓使用者實測 Google 登入。

## 已完成
- [x] firestore.rules **已部署並 byte 級驗證**（線上 release `05f9b203` == 本地修補版，含 operationLogs schema + userMappings 防提權兩項資安修補）；`--dry` 語法驗證通過
- [x] 更正 PLAN §0.5/§0 過時的「尚未部署」備註（commit `e00e89f`）
- [x] V2 最新版部署到 **preview 子站**（commit `2f989d7`）：https://uplilt31311227.github.io/STsystem-preview/
- [x] 建立回朔機制：備份分支 `backup-pre-phase1-20260625`、回朔點 `90db3b4`，並寫入 DEPLOYMENT.md
- [x] **修復登入按鈕失效**：app.js 兩處重複宣告（`const esc` vs `function esc`、`subjectList` 整塊重複）造成 SyntaxError 中斷主程式 → 已移除重複，`node --check` 通過（commit `2f989d7`）
- [x] 確認正式站 master **不受影響**（其 app.js 語法正常）

## 目前進度
Phase 1 程式碼 + rules 全部到位、preview 已可線上載入。**等使用者強制重整後實測 Google 登入是否恢復**。

## 未完成
- [ ] 使用者 Ctrl+Shift+R 重整 preview → 確認 Google 登入按鈕恢復正常
- [ ] 三角色登入驗收（director / section_chief / teacher）：對照 body class / 徽章 / 頁籤可見性
- [ ] 課表匯入串接測試（CSV → toast → 教師管理高亮）
- [ ] 規則攻擊測試（teacher 直接 POST / 改 role / 提權 / 日誌寫入 的 DENY/ALLOW）
- [ ] （可選）把本次登入 bug 記入 docs/ISSUES_LOG.md

## 下一步
1. 使用者回報 preview 重整後登入是否正常；若仍異常，抓 DevTools Console 紅字定位
2. 登入恢復後，依上述清單逐項做 Phase 1 三角色 + 規則攻擊實機驗收
3. Phase 1 驗收通過 → 進 Phase 2（schoolId→inhu 驗證）/ Phase 3（三種審核流程分支）

## 注意事項
- **本機無法 Google 登入**（Firebase OAuth 不授權 localhost）→ 一律用 preview 子站線上測
- Firebase 設定皆正常：Google/Email provider 已啟用、`uplilt31311227.github.io` 已在 Authorized domains
- Console 中 `inpage.js`/`evmAsk.js`/`Cannot redefine property: ethereum` 是瀏覽器加密錢包擴充，與本系統無關
- **回朔指令**：`git push preview 90db3b4:main -f`
- 部署流程：`git push preview feature/permission-system:main`；origin 與 preview 兩端皆已同步至 `2f989d7`
- 正式站 master 仍 v1.x，PLAN 規定 V2 通過兩週試用前不可碰 master
