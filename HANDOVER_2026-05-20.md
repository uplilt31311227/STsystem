# 工作交接紀錄

> 日期：2026-05-20
> 專案：STsystem（國中調代課自動化系統）
> 分支：master（v1.11.0 開發中）+ feature/permission-system（已合 v1.10.0）

## 當前計畫
- v1.10.0 衝突檢查強化 → **已完成上線**
- v1.11.0 週彙整通知單 PDF → **本地 commit、迭代修飾中、尚未 push**

## 已完成
- [x] v1.10.0 三項衝突檢查（推薦引擎排除已派代/調課教師、送出 fail-safe 攔截、課表上傳班級衝突）
- [x] v1.10.0 push origin/master（commit `8f2a9d7`）
- [x] v1.10.0 同步 merge 進 feature/permission-system（commit `70cc91b`）並 push
- [x] v1.11.0 初版實作（commit `09be157` 本地）：教學組頁 + 班/原任課/代課頁 + modal + UI
- [x] v1.11.0 迭代（**未 commit**）：
  - 課表未變動格顯示原任課（老師頁顯示班/科、班級頁顯示教師/科）
  - 變動格同時顯示原任課 + 代/調入老師
  - 排版改為 A4 橫向左右兩式直切（551×794 半頁、垂直虛線分隔）

## 目前進度
- v1.10.0：100% 完成
- v1.11.0：約 90%。功能實作完整、語法通過、待使用者瀏覽器手測排版後 commit + push

## 未完成
- [ ] 瀏覽器強制重整測試最新 v1.11.0 PDF（http://localhost:8000）
- [ ] 視覺 OK 後 commit v1.11.0 後續修飾（amend 09be157 或新 commit）
- [ ] push origin/master（需使用者明確授權，auto mode 會擋）
- [ ] merge v1.11.0 進 feature/permission-system（CHANGELOG 預期會 conflict）

## 下一步
1. 使用者在瀏覽器測 `http://localhost:8000` → 調代課紀錄 → 列印本週彙整
2. 確認 PDF 排版（橫向左右兩式、變動格雙師、未變動格原任課）
3. 若需再調 → 告知方向繼續迭代；若 OK → commit + push
4. 同步 merge 到 feature/permission-system

## 注意事項
- **HTTP server** 仍在背景跑：bash ID `beaf18xc0`、port 8000（結束工作時 kill）
- **v1.11.0 commit `09be157` 是初版**，後續多次 Edit 未 commit；新 commit 應在原訊息基礎上補「排版迭代」說明
- **auto mode 會擋 push 到 master**，使用者要明確說「請 push 到 origin/master」才放行
- **CHANGELOG.md merge 衝突**：feature/permission-system 與 master 在 unreleased 區塊歷次都會 conflict，按 master 內容為主、保留 V2 alpha 區塊
- **計畫檔**：`C:\Users\uplil\.claude\plans\recursive-kindling-lerdorf.md`（v1.11.0 plan，含驗證步驟）
- **PDF 半頁尺寸**：551×794px（A5 直立），整頁 1123×794（A4 橫向）；改尺寸時注意 `createCombinedHalfPageHTML` 與 sheet 內部 width/height 要同步
