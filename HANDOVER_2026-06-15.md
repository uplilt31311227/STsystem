# 工作交接紀錄

> 日期：2026-06-15
> 專案：STsystem（國中調代課自動化系統）

## 當前計畫
新增「九年級畢業停用課程」功能：讓九年級畢業後，課表上的 9年X班 課程不再把老師判為忙碌、不再擋住調代課。已完成並上線 GitHub Pages（v1.12.0）。

## 已完成
- [x] 將誤用的 `/deep-research` 導正為功能開發
- [x] 確認 3 個設計決策（保留資料標記停用 / 手動開關 / 班級名稱前綴判斷）→ 存 `docs/PLAN_grade9_graduation.md`
- [x] 核心實作：`dataManager.js`（settings.grade9Disabled、isGraduatedClass、getActiveScheduleData + 5 處持久化）
- [x] 串接：`app.js`（代課推薦、getBusyTeachers、代課送出攔截、單次調課、批次調課全改用有效課表 + 開關 UI）
- [x] UI/樣式：`index.html`（學年度管理開關）、`style.css`（停用課灰底刪除線）
- [x] 18 項單元測試（`test/test-grade9.mjs`）全通過
- [x] 對抗式 code review（修掉編輯器停用課游標 UX 矛盾）
- [x] 更新 CHANGELOG / ISSUES_LOG（master 與 feature 兩版本）
- [x] cherry-pick 乾淨上線 master（不帶 V2）→ Pages 部署成功（v1.12.0，commit `decc1d7`）
- [x] 打 tag `v1.12.0-stable`、push feature 分支備份

## 目前進度
功能 100% 完成並已上線正式站。剩「線上版人工驗收」。

## 未完成
- [ ] 線上版人工驗收（https://uplilt31311227.github.io/STsystem/ → 設定頁「九年級已畢業」開關）
- [x] ~~雲端同步翻轉 grade9Disabled 時不會即時重繪已開啟的推薦/調課面板~~ → **已於 2026-06-18 修復（v1.13.2，commit `5711894`）**，並順帶修掉 `renderEditorSchedule` latent bug 與即時同步監聽器洩漏。計畫：`docs/PLAN_grade9_realtime_refresh.md`。

## 後續更新（2026-06-18）
- v1.13.2 已上線 master（tag `v1.13.2-stable`），剩線上人工驗收。
- `feature/permission-system` 已合併 master 取得 v1.13.1/v1.13.2 修復（merge `59ef017`）。
- V2 現況見 `docs/PLAN_v2.0.0.md`：裁決已定（接續）、Phase 1 程式碼完成，**卡 Phase 1 實機驗收**（Firebase Console 啟用 Email/Password + 三角色登入測試）。

## 下一步
1. 開正式站 Ctrl+Shift+R 強制刷新，走一遍驗收：匯入含九年級課表 → 勾選開關 → 確認九年級課變灰底、原被擋住的老師恢復可代課 → 重整後開關狀態保持
2. 驗收無誤即結案；若有文案/位置/格式調整再回報
3. 回到 feature/permission-system 繼續 V2 權限系統開發（與本功能無關）

## 注意事項
- **分支**：master 已含並部署九年級功能；目前工作分支為 `feature/permission-system`（V2 開發中，已 push 備份）
- **V2 未上線**：正式站只有「穩定版 + 九年級功能」，未完成的 V2 權限系統完全沒被帶上 master
- **回滾**：可 `git checkout v1.12.0-stable`（或 `v1.11.0-stable`）回滾
- **本機伺服器**：`http://127.0.0.1:8000`（python http.server）仍在背景執行，可供比對；不需要時可關閉
- **判斷格式**：九年級涵蓋 9年X班 / 九年X班 / 9XX（如 901）；月結算與 PDF 刻意維持原始課表以保留歷史
